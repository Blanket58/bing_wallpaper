packages<-.packages(all.available=T)
if(!any("RCurl" == packages)) install.packages("RCurl",repos = "https://mirrors.tuna.tsinghua.edu.cn/CRAN/")
if(!any("XML" == packages)) install.packages("XML",repos = "https://mirrors.tuna.tsinghua.edu.cn/CRAN/")
if(!any("stringr" == packages)) install.packages("stringr",repos = "https://mirrors.tuna.tsinghua.edu.cn/CRAN/")
if(!any("yaml" == packages)) install.packages("yaml",repos = "https://mirrors.tuna.tsinghua.edu.cn/CRAN/")
library(yaml)
library(RCurl)
library(XML)
library(stringr)
modules<-readLines("../configs/modules.txt") %>% str_split(" +")
modules<-sapply(modules[-c(1,2)],function(x) x[1])
if(!any("pywin32" == modules)) system("pip install pywin32")
cat("All dependencies have been successfully installed.\n")
main<-readRDS("../configs/www")
eval(parse(text = main))
cat("Done.\n")
